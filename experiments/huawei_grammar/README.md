# Experimental Huawei grammar

CommandTree and CommandProfile/ProfileOverlay are research scaffolding, not runtime
validation. They are excluded from the netconfiglint package and standalone builds.
A future integration needs documented syntax branches, source-preserving parsing,
profile fact resolution and negative/unknown cases before entering production.
