# v2.0 acceptance record

This file records commands and results for the release candidate. It distinguishes source tests,
compiled-runtime checks, native installer checks, signatures, and unavailable environments.

## Source and UI gates

- Windows source gate: Ruff format/check passed, mypy passed, and the full suite passed before
  final package compilation. The final lifecycle regression is included in the 525-test suite.
- Debian 13.3 source gate: Ruff format/check, mypy, pip dependency check, and `525 passed` on
  Python 3.13.5 / Qt 6.11.2.
- Debian offscreen smoke: v2.0.0, Software renderer, three exports, zero captured QML errors,
  and zero stderr bytes after deterministic QML teardown.
- Bounded repository scan: 632 tracked/intended files, 888 reachable historical blobs, zero
  current secret/path findings; one pre-v1.5 historical author identity remains explicitly grandfathered.

## Windows

- Compiled standalone passed native Direct3D 11 and forced Software smoke on Python 3.13.2 /
  Qt 6.11.2 with three exports and no QML errors.
- Portable ZIP passed checksum, x64/GUI subsystem, fresh Chinese-and-space extraction path,
  native/software smoke, restricted non-admin token, system-only PATH, no console child,
  no unwanted history file, responsive-window, and clean-exit checks.
- Final portable SHA-256: `b0b5e9e91b6050e195dd44fae9e4c8496b35e2b62dd20d3b47402bea7ac34b2c`.
- WiX 3.14.1 MSI compiled with standard ICE validation. Final unsigned MSI SHA-256:
  `8c943efa0c48b4a24b15a5d2b951c777e451af96ea789e1d411de4588b40b5bf`.
- Pending: MSI install-directory, shortcuts, launch, keep-history uninstall, delete-all-data uninstall.
- Signature status: no trusted local certificate was available at implementation time; do not mark signed without a later Valid Authenticode result.

## Debian 13

- Source/runtime checks above passed on the authorized Debian 13 VM. Pending: install the amd64
  DEB produced by the qualified Python 3.13.2 CI builder, verify launcher, and exercise keep/delete uninstall paths.

## macOS

- Source and CI packaging can be checked, but no user macOS environment is available. Do not call
  this user-hardware or universal compatibility acceptance. Signing/notarization requires Apple identities.
