# v1.5 发布验收

本文件只记录已有证据；源码回归、编译后运行和最终下载包分开验收。
审计条目对应关系见 [修复汇总](v1.5-audit-repairs.md)。

## 已完成的阶段证据

| 阶段 | 实际结果 |
| --- | --- |
| P1 / P3 | 277 项测试，Ruff / mypy，Windows 原生 QML 和三个导出通过 |
| P2 | Qt 6.9.0 和 6.11.2 下各 413 项通过；Ruff / mypy 通过 |
| P2 Windows 源码 | Direct3D11 与 Software 两条烟雾路径通过，QML 错误 0 |
| P1 / P2 / P3 合并 | 默认片段模式、筛选与行跳转、导出、历史、覆盖、取消交接共 30 项通过 |
| 打包许可门禁自身 | 旧分发目录 / ZIP 清单验证通过，篡改与缺失许可反例被拒绝 |
| Git 传输后许可完整性 | 233 份固定上游许可材料的 SHA256 匹配 |
| 华为专项 | 73 个新增参数化用例；关联集合 366 通过 / 1 个已修复回归，随后 7 项与 14 项定向验证通过；Ruff / mypy 通过 |
| 私有归档（华为专项） | 仅运行一次，40 / 40 完成、0 失败；最后 group 保护以合成测试验证，详见专项报告 |

上述通过结果直接沿用，没有因额度恢复而重复执行。华为专项改动及最终构建产生新内容时，
仅补充必要的关联验证和产物验收。

## 三平台门禁

GitHub CI 覆盖 Windows、Linux 和 macOS，Python 3.12 / 3.13 与 Qt 6.9 / 6.11 的运行边界。
严格类型检查使用发行版 Qt 6.11.2 类型声明；Qt 6.9 的类型声明遗漏了可运行的 Property 装饰器和
invokeMethod 重载，因此其运行测试仍保留，类型检查不依赖这些旧声明。

三个系统分别编译后在独立 Unicode / 空格目录中运行原生与软件渲染烟雾。
Linux CI 使用 Xvfb；CI 虚拟设备结果不代表所有物理显卡、显示缩放或桌面会话。
Linux / macOS 的编译目录仅供 CI 运行验收，不作为 Release 资产分发；本次分发许可和完整
SBOM 门禁针对用户要求的 Windows 便携包。Unix 的重定位 / 签名会改变库字节，不能将其冒充为
Windows 门禁所验证的原始 wheel 二进制。

最终 CI 运行、编译与下载包结果在发布收尾时填写。

## Windows 便携包验收范围

最终 ZIP 应通过完整解压、SHA256、x64 GUI 子系统、PE 产品 / 文件版本及软件内版本验证。
启动环境只保留 Windows 系统 PATH，去除 Python / Qt 开发环境变量；验收原生与软件渲染、
默认配置片段模式、三种导出、普通窗口响应和正常退出。

同时验证非提权进程。当前测试机使用受限 LUA token，实际子进程的 TokenElevation=false；
该证据不等同于另一台机器或独立标准用户登录。包不依赖预装 Python。

## 发布隐私与许可

源码、提交内容、作者身份及 ZIP 分别检查；报告只记录位置和类别，不输出敏感值。
新增提交使用项目 noreply 身份，原始设备配置、个人机器审计和测试导出不进入发布内容。

Qt 官方二进制中已有的构建机路径只在与已核实官方 wheel 完全同哈希时单独记录；应用代码和
修改过的二进制不能使用该例外。固定上游许可、实际分发文件清单及 SBOM 在最终 ZIP 上再次核对。

既有公开历史中存在一条早于本次发布的个人作者邮箱记录。本次没有新增此类身份，也没有改写公开
历史；因此本报告不声称旧历史完全不存在个人资料。
