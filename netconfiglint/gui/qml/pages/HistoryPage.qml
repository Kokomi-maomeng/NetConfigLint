import QtQuick
import theme 1.0
import "../components"

EmptyState {
    title: "History is planned for P2"
    description: "v1.0 Beta does not persist configuration text or analysis history by default."
}
